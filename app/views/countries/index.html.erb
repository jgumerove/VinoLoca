 <% if @countries.count < 1 %>
 <h3>there are no wines in your collection / add your first one by clicking below<h3>
   <% else %>
<h1>Your wine collection consists of the following countries:</h1>
<table>
  <tr>
     <th>Country</th>
     <th>Number of wines</th>
  </tr>
  <tr>
    <% @countries.uniq.each do |c| %>
        <td><%= link_to c.name, country_wines_path(c.id) %></td>
        <td><%= c.wines.count %></td>
 </tr>
    <% end %>
 </table><br/>
 <% end %>
<%= link_to "Add a wine and a country to your collection", wines_new_path %>

 
