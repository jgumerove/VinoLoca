<h1>Your Wine Collection Consists of the Below Countries</h1>
<table>
  <tr>
     <th>Country</th>
     <th>Number of wines</th>
  </tr>
  <tr>
    <% @countries.uniq.each do |c| %>
        <td><%= link_to c.name, country_wines_path(c.id) %></td>
        <td><%= c.wines.count %></td>
 </tr>
    <% end %>
 </table><br/>

 <%= link_to "Add A wine to your collection", wines_new_path %>
